{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python -m app.cron process_payouts",
    "cronSchedule": "0 14 24 5 *",
    "restartPolicyType": "NEVER"
  }
}
